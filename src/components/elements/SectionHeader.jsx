

const SectionHeader = ({text}) => {
  return (
    <div className="flex m-auto justify-center py-8">
        <h2 className="text-white text-4xl">{text}</h2>
    </div>
  )
}

export default SectionHeader